<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tux.dms">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DMS"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.admin.AdminActivity"
            android:exported="true" />
        <activity
            android:name=".activity.operator.UserActivity"
            android:exported="true" />
        <activity
            android:name=".activity.creator.TicketCreatorActivity"
            android:exported="true" />
        <activity
            android:name=".activity.register.RegisterActivity"
            android:exported="true" />
        <activity android:name=".activity.login.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>